import { createTheme } from "@mui/material";

export const theme1 = createTheme({
    palette: {
        primary: {
            main: '#25131a'
        },
        secondary: {
            main: '#3d253b',
            contrastText: '#fff'
        },
        background: {
            default: '#6e4a48'
        }
    }

})